<% @users.each do |user| %>
  <%= link_to users_show_path(user.id) do %>
    <strong><%= user.fullname %></strong> | 
    <small>@<%= user.username %></small><br>
    <%= user.email %><br><br>
  <% end %>
<% end %>
