<%= form_with model: @opinion do |form| %>

  <div class="field">
    <%= form.label 'Opinion', hidden: true %>
    <%= form.text_area :body, :rows=>5, :cols=>50, placeholder: "What's on your mind?", required: true, autofocus: true, class: 'new-opinion-input', id: 'opiniontext' %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div><br>
<% end %>
